{
  "name": "VisitScope static site",
  "install": "python3 --version",
  "terminals": [
    {
      "name": "static-server",
      "command": "python3 -m http.server 8080 --bind 0.0.0.0",
      "description": "Serves the VisitScope static site (index.html, brief.html, assessment.html) on port 8080."
    }
  ],
  "ports": [
    {
      "name": "web server",
      "port": 8080
    }
  ]
}
